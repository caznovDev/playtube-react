CREATE TABLE IF NOT EXISTS videos(id INTEGER PRIMARY KEY AUTOINCREMENT,title TEXT,thumbnail_url TEXT,video_url TEXT,channel_name TEXT,views INTEGER DEFAULT 0);
CREATE TABLE IF NOT EXISTS models(id INTEGER PRIMARY KEY AUTOINCREMENT,name TEXT,avatar_url TEXT,bio TEXT);